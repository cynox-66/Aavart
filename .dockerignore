.git
.github
.venv_pdf
**/__pycache__
**/.pytest_cache
**/.ruff_cache
**/.mypy_cache
**/.venv

# Generated JS/Next.js artifacts. These are rebuilt inside the image
# (see apps/web/Dockerfile), so the host copies must never enter the build
# context -- `next dev` writes into .next/cache concurrently, which stalls
# the context upload.
**/node_modules
**/.next
**/.turbo
**/*.tsbuildinfo

**/test-results
**/playwright-report
**/coverage
.pnpm-store
.env
